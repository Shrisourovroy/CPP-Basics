#include<bits/stdc++.h>
using namespace std;
int fac(int x){
    if(x==0){
        return 1;
    }
    else return x*fac(x-1);
}
int main()
{
    int n;
    cin>>n;
    for(int j=0; j<n; j++)
    {
        int m;
        cin>>m;
        cout<<fac(m)<<endl;
    }
    return 0;
}
